For execute the program in the terminal only need run pipeline.py withouth params

pipeline.py executes the scrapper then clean all the data and then save it into a database

for now it only has info from the newsPaper eluniversal if you want more they need to be added in the file Scrapper/config.yaml and also in the list news_sites_uid in pipeline.py